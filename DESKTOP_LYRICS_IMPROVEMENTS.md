# 桌面歌词功能改进说明

## 改进概览

本次更新为桌面歌词功能增加了更多可配置参数，并实现了鼠标滚轮手动调节缩放比例的功能。

## 新增参数

### 1. 窗口缩放（desktopLyricScale）
- **范围**: 0.5 - 2.0
- **默认值**: 1.0
- **说明**: 控制整个歌词窗口的缩放比例，1.0 为正常大小
- **特性**: 支持鼠标滚轮实时调节（在歌词窗口上滚动即可）

### 2. 行间距（desktopLyricLineHeight）
- **范围**: 1.0 - 2.0
- **默认值**: 1.3
- **推荐值**: 1.2 - 1.5
- **说明**: 控制歌词行与行之间的间距

### 3. 文字阴影强度（desktopLyricShadowIntensity）
- **范围**: 0 - 1.0
- **默认值**: 0.5
- **说明**: 控制歌词文字的阴影效果强度，0 为无阴影

### 4. 翻译字号比例（desktopLyricTranslationScale）
- **范围**: 0.4 - 1.0
- **默认值**: 0.58
- **说明**: 控制翻译文本相对主歌词的大小比例

## 现有参数优化

原有的 8 个参数保持不变，并进行了优化：

1. **字体选择** - 支持 13 种字体风格
2. **字重** - 支持 6 个粗细级别
3. **文字颜色** - 自定义主歌词颜色
4. **描边颜色** - 自定义描边颜色
5. **字号大小** - 24-72px 可调
6. **描边宽度** - 0-5px 可调
7. **文字透明度** - 0-1 可调
8. **背景透明度** - 0-1 可调，支持毛玻璃效果

## 功能改进

### 动态样式应用
- 所有设置现在通过 JavaScript 动态应用到歌词窗口
- 移除了 CSS 中的硬编码样式
- 设置变更后立即生效，无需重启窗口

### 鼠标滚轮缩放
- 在歌词窗口上使用鼠标滚轮可实时调节缩放比例
- 滚轮向上：放大（+0.1）
- 滚轮向下：缩小（-0.1）
- 范围限制：0.5 - 2.0
- 锁定状态下禁用缩放功能

### 平滑过渡动画
- 所有样式变更都添加了平滑的过渡动画
- 缩放操作响应迅速（0.15s）
- 样式调整柔和自然（0.2s）

## 技术实现

### 文件修改列表

1. **public/admin.html**
   - 新增 4 个设置区块（窗口缩放、阴影与特效）
   - 新增 8 个表单控件（range + number 双向绑定）

2. **public/js/admin/desktop-lyric.js**
   - 扩展 `collectDesktopLyric()` 收集新参数
   - 扩展 `loadDesktopLyricSettings()` 加载新参数
   - 新增 4 个 `bindRangePair()` 绑定

3. **public/lyric-window.js**
   - 新增 `lyricSettings` 状态管理
   - 新增 `loadSettings()` 从服务器加载设置
   - 新增 `updateSettings()` 更新设置状态
   - 新增 `applyStyles()` 动态应用所有样式
   - 新增 `setupWheelZoom()` 鼠标滚轮缩放功能
   - 增强 `renderLyricState()` 配合动态样式

4. **public/styles-playback.css**
   - 移除 `.lyric-window-surface` 的硬编码样式
   - 移除 `.lyric-window-line` 的硬编码字体样式
   - 移除 `.lyric-window-translation` 的硬编码字体样式
   - 添加 `transition` 和 `will-change` 优化性能

5. **src/storage/settings-store.js**
   - 在 `DEFAULT_SETTINGS` 中新增 8 个桌面歌词参数
   - 确保数据库初始化时写入默认值

## 使用方法

1. **通过设置面板调节**
   - 打开管理后台 → 设置 → 桌面歌词设置
   - 调节各项参数并保存
   - 桌面歌词窗口会自动应用新设置

2. **通过鼠标滚轮调节**
   - 打开桌面歌词窗口
   - 在窗口上滚动鼠标滚轮
   - 向上滚动放大，向下滚动缩小
   - 缩放范围：50% - 200%

3. **锁定功能**
   - 锁定歌词窗口后，鼠标滚轮缩放将被禁用
   - 可防止误操作

## 最佳实践建议

### 推荐配置组合

**清晰模式**（适合投屏/录播）：
- 字号：42-48
- 描边宽度：2.5-3
- 文字透明度：1.0
- 背景透明度：0.2-0.3
- 阴影强度：0.6-0.8

**简洁模式**（适合直播）：
- 字号：36-40
- 描边宽度：2
- 文字透明度：0.9
- 背景透明度：0
- 阴影强度：0.5

**柔和模式**（适合卡拉OK）：
- 字号：40-44
- 描边宽度：1.5-2
- 文字透明度：0.85
- 背景透明度：0.15
- 阴影强度：0.4

## 兼容性

- ✅ Windows 11/10
- ✅ Electron 桌面环境
- ✅ 现有所有主题配置
- ✅ 现有播放器功能
- ✅ 向后兼容（旧数据库自动添加默认值）

## 注意事项

1. 首次使用时，如果桌面歌词窗口已经打开，需要关闭后重新打开以加载新功能
2. 鼠标滚轮缩放的比例不会自动保存，重新打开窗口后会恢复到设置面板中配置的值
3. 如需永久保存缩放比例，请在设置面板中调节"整体缩放"参数
4. 极端的参数组合可能导致显示效果不佳，建议遵循推荐范围

## 性能优化

- 使用 CSS `will-change` 优化 transform 性能
- 使用 `transition` 而非动画减少重绘
- 缩放操作使用硬件加速的 `transform: scale()`
- 样式应用批量处理，减少 DOM 操作次数

## 未来可能的扩展

- [ ] 保存鼠标滚轮调节的缩放值到设置
- [ ] 支持键盘快捷键调节参数
- [ ] 预设样式模板一键切换
- [ ] 歌词位置拖拽调节
- [ ] 多屏幕支持
