{
  "name": "bilibili-live-song-plugin",
  "version": "3.0.14",
  "private": true,
  "license": "UNLICENSED",
  "description": "Local Bilibili live song request assistant MVP.",
  "author": "Aurora",
  "main": "src/electron/main.js",
  "scripts": {
    "start": "node src/server.js",
    "desktop": "electron .",
    "check": "node scripts/check-js.js",
    "test": "node --experimental-vm-modules --test --test-concurrency=1",
    "make:icon": "node scripts/create-icon.js",
    "dist:win": "npm run make:icon && electron-builder --win nsis --x64",
    "dist:win:local": "npm run make:icon && set ELECTRON_SKIP_BINARY_DOWNLOAD=1 && electron-builder --win nsis --x64 --config.electronDist=node_modules/electron/dist",
    "release:win": "node scripts/publish-release.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@jixun/qmweb-sign": "2.0.3",
    "electron-updater": "^6.8.4",
    "qrc-decoder": "1.0.2"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "^26.11.1"
  },
  "build": {
    "appId": "com.aurorawhisperer.bilibili-live-song-plugin",
    "productName": "点歌助手",
    "artifactName": "bilibili-live-song-plugin-setup-${version}.${ext}",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "files": [
      "src/**/*",
      "public/**/*",
      "package.json"
    ],
    "asar": true,
    "npmRebuild": false,
    "win": {
      "icon": "build/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "artifactName": "bilibili-live-song-plugin-setup-${version}.${ext}",
      "include": "build/installer.nsh",
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "shortcutName": "点歌助手",
      "uninstallDisplayName": "点歌助手",
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "publish": [
      {
        "provider": "github",
        "owner": "AuroraWhisperer",
        "repo": "Request-song",
        "releaseType": "release"
      }
    ]
  }
}
